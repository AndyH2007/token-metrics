export type IndexItem = { id: string; name: string; symbol?: string };
export type IndicatorItem = { id: string; name: string; category?: string };
export type SeriesPoint = { t: string; v: number };
